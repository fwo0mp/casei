[project]
name = ""
version = "0.0.1"
requires-python = "~=3.10"
dependencies = [
    "django",
    "django-object-tools",
    "django-south-admin",
    "south",
    "uwsgi",
    "python-memcached",
    "psycopg2",
    "bs4",
    "django-debug-toolbar",
    "pymemcache",
]

[dependency-groups]
dev = []

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"

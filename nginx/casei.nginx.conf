upstream uwsgi {
    server unix:/tmp/uwsgi/casei.sock;
}

server {
    listen 80;
    server_name 127.0.0.1;

    location /static/ {
        alias /casei/static/;
    }

    location / {
        include /etc/nginx/uwsgi_params;
        uwsgi_pass uwsgi;
    }
}